---
title: Simulations advanced (STUB — DRAFT)
subtitle: Configure tool mocks, hooks, and testing strategies for simulations.
description: "DRAFT stub. Advanced simulation testing: tool mocks, simulation hooks, reusing structured outputs, testing strategies, and CI/CD integration."
slug: observability/simulations-advanced-stub
---

{/*
  DRAFT STUB — Feature Guide type. Based on the Linear "Feature Guide Template"
  (vapi/document/feature-guide-template-f3f0725d8388). Replaces simulations-advanced.mdx at launch.
  Legend: [OK] confirmed  [PENDING] confirm  [TODO] to write
  CONCEPT CAP: "How it works" ~150 words max; deeper concept graduates to the overview page.
  ⚠ TYPE CHECK: this page currently bundles several tasks (tool mocks, hooks, CI/CD). Per the
    "one page = one type" rule it may need to SPLIT into separate How-to Guides at the definitive
    pass. Keeping as one Feature-Guide stub for now — revisit.
*/}

<Warning>
  Draft stub for the Simulations GA rewrite. Not for publication.
</Warning>

{/* LEAD (no heading): definition-style sentence + when to use. 2–3 sentences. */}
{/* [TODO] "Advanced simulations let you mock tools, trigger lifecycle hooks, and reuse
   structured outputs to test specific paths deterministically. Use these once your basic
   suite runs and you need finer control." */}

## How it works
{/* [OK] CAPPED ~150 words — how advanced scenario config layers onto a basic simulation */}

## Tool mocks
{/* [OK] concept keep; [PENDING] re-verify UI location in new suite editor (no Scenarios tab now) */}

## Simulation hooks
{/* [OK] simulation.run.started / .ended via per-simulation webhook hooks; voice-only (vapi.websocket) */}
{/* [PENDING] FIX STALE NAV — old page says "Simulations → Scenarios → Hooks"; no Scenarios tab now */}
{/* [OK] payload envelope { "event": ... } (NOT server-url message.type) */}

## Using existing structured outputs
{/* [OK] keep; re-anchor to new Scenario eval UI */}

## Testing strategies
{/* [OK] smoke / regression / edge case (product-agnostic) */}

## CI/CD integration
{/* [PENDING] depends on API exposure; assume cURL correct for now, verify at final API */}

## Verify it works
{/* [OK] observable success signal per config (e.g. mocked tool returns your value; hook webhook received) */}

## Related
{/* [OK] wayfinding */}
<CardGroup cols={2}>
  {/* [TODO] Card → Simulations overview (concept) */}
  {/* [TODO] Card → Simulations quickstart / future Reference */}
</CardGroup>
