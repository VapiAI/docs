---
title: Simulations quickstart (STUB — DRAFT)
subtitle: Run your first AI-tester simulation against an assistant in minutes.
description: "DRAFT stub. Create a simulation suite, add a scenario and personality, run an AI tester against your assistant or squad, and review pass/fail results."
slug: observability/simulations-quickstart-stub
---

{/*
  DRAFT STUB — Quickstart type. Based on the Linear "Quickstart Template"
  (vapi/document/quickstart-template-3c72691b97ab). Replaces simulations-quickstart.mdx at launch.
  Legend: [OK] confirmed in preview  [PENDING] confirm  [TODO] to write  [REMOVED] drop from old page
  Rules from template: happy path only, one step = one action (imperative), runnable tagged code,
  keep prerequisites short, link edge cases under Next steps.
  MULTI-METHOD TABS: ASK AUTHOR which methods (Dashboard / API). API cURL PENDING until exposed.
*/}

<Warning>
  Draft stub for the Simulations GA rewrite. Not for publication.
</Warning>

{/* LEAD (no heading): what works by the end + rough time. 2–3 sentences. */}
{/* [TODO] "In this quickstart, you'll run an AI tester against your assistant and see a
   pass/fail result in about 10 minutes." */}

## What you'll build
{/* [OK] one line — a simulation suite that runs a scripted tester against your assistant/squad */}

## Prerequisites
{/* [OK] Vapi account; at least one assistant or squad to test */}
{/* [PENDING] de-emphasize API key until API exposed */}

## Steps
{/* Suite-first flow. NEW ordering vs old page. */}
<Steps>
  {/* [OK] Step: Create a simulation suite (Simulations → Suites → Create suite; Name + Targets) */}
  {/* [OK] Step: Add a simulation (Scenario name + Intent; Personality preset + Behavior; Who starts first) */}
  {/* [PENDING] Step: Set evaluation criteria (structured outputs + comparators) — confirm UI location */}
  {/* [PENDING] Step: Run the suite (transport voice/chat, iterations) — DO NOT run in preview */}
  {/* [PENDING] Step: Review results (Runs → run detail, pass/fail, transcript, recording) */}
</Steps>

## Next steps
{/* [OK] link everything kept out of the happy path */}
<CardGroup cols={2}>
  {/* [TODO] Card → Simulations advanced (tool mocks, hooks, strategies) */}
  {/* [TODO] Card → Simulations overview (what it is + vs Evals) */}
</CardGroup>

{/*
  REMOVED vs current page:
  [REMOVED] standalone Create-a-Personality / Create-a-Scenario / Create-a-Simulation steps (now inline in suite)
  [REMOVED] 5-tab nav model (now only Suites + Runs)
  [REMOVED] cURL tabs to /eval/simulation/* (API not exposed — park; assume-correct per decision)
*/}
