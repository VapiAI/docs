---
title: OpenAI
subtitle: Use OpenAI for text-to-speech in Vapi.
description: Configure OpenAI as your Vapi assistant's text-to-speech provider, including TTS models and voice selection.
slug: providers/voice/openai
---

OpenAI provides text-to-speech for Vapi voice agents.

You can use OpenAI through Vapi's default integration, or connect your own account in [Integrations](/providers/integrations).

## Supported capability

| Capability | Provider value |
| --- | --- |
| Text-to-speech | `openai` |

## Text-to-speech

Set `voice.provider` to `openai`, `voice.model` to a TTS model (`tts-1`, `tts-1-hd`, or `gpt-4o-mini-tts`), and `voice.voiceId` to a voice. Browse and copy a voice's ID from the [Voice Library](/providers/voice/overview); the default voice is `alloy`.

```bash
curl -X PATCH "https://api.vapi.ai/assistant/ASSISTANT_ID" \
  -H "Authorization: Bearer $VAPI_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "voice": { "provider": "openai", "model": "gpt-4o-mini-tts", "voiceId": "alloy" }
  }'
```

For additional API configuration options, review the [`OpenAIVoice` fields](/api-reference/assistants/create#request.body.voice.OpenAIVoice) in the Create Assistant API reference.

## Supported languages

OpenAI text-to-speech has no separate language setting. The model speaks the language of the input text.

## Related

<CardGroup cols={2}>
  <Card title="OpenAI language models" icon="brain" href="/providers/model/openai">
    Configure OpenAI for language models.
  </Card>
  <Card title="OpenAI speech-to-text" icon="wave-square" href="/providers/transcriber/openai">
    Configure OpenAI for speech-to-text.
  </Card>
  <Card title="Voice providers" icon="volume-high" href="/providers/voice/overview">
    Compare every voice provider Vapi supports.
  </Card>
</CardGroup>
