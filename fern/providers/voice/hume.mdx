---
title: Hume
subtitle: Use Hume's expressive text-to-speech voices for your Vapi voice agent.
description: Connect Hume to Vapi to use its expressive Octave text-to-speech voices for your voice agent. Covers the models and how to select a voice in the API.
slug: providers/voice/hume
---

Hume is a text-to-speech provider you can use for your voice agent's voice. Connect Hume to use its Octave voices on a Vapi assistant. This page covers what Hume is and how to select a voice.

## What is Hume

Hume is a text-to-speech provider whose Octave models generate expressive, emotionally aware speech. On Vapi, you select a Hume voice for an assistant, and Hume also supports custom voices.

You can use Hume through Vapi's default integration, or use your own Hume account by connecting it in [Integrations](/providers/integrations).

## Text-to-speech

Set `voice.provider` to `hume`, `voice.model` to a Hume model (`octave` or `octave2`), and `voice.voiceId` to a voice. Hume offers many voices and supports custom voices, so browse and copy a voice's ID from the **Voice Library** in the [Vapi Dashboard](https://dashboard.vapi.ai).

```bash
curl -X PATCH "https://api.vapi.ai/assistant/ASSISTANT_ID" \
  -H "Authorization: Bearer $VAPI_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "voice": { "provider": "hume", "model": "octave", "voiceId": "VOICE_ID" }
  }'
```

## Related

<CardGroup cols={2}>
  <Card title="Hume documentation" href="https://dev.hume.ai">
    The provider's own API reference.
  </Card>
  <Card title="Voices" href="/providers/voice/overview">
    Compare every text-to-speech provider Vapi supports.
  </Card>
</CardGroup>
