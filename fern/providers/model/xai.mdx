---
title: xAI
subtitle: Use xAI's Grok models, voices, and speech-to-text in your Vapi voice agents.
description: Connect xAI to Vapi to use Grok language models, xAI text-to-speech voices, and xAI speech-to-text. Covers supported models, voices, and languages.
slug: providers/model/xai
---

xAI is a provider you can use for three parts of a Vapi voice agent: the language model (Grok), the voice (text-to-speech), and transcription (speech-to-text). This page covers what xAI is and the options each capability exposes.

## What is xAI

xAI builds the Grok family of large language models and also offers text-to-speech and speech-to-text. On Vapi, you can use xAI for any combination of the model, voice, and transcriber on a single assistant.

You can use xAI through Vapi's default integration, or use your own xAI account by connecting it in [Integrations](/providers/integrations).

## Language model

Set `model.provider` to `xai` and `model.model` to a Grok model.

| Model | `model` value |
| -- | -- |
| Grok 4.3 Non Reasoning | `grok-4.3` |
| Grok 4.20 Reasoning | `grok-4.20-0309-reasoning` |
| Grok 4.20 Non Reasoning | `grok-4.20-0309-non-reasoning` |

```bash
curl -X PATCH "https://api.vapi.ai/assistant/ASSISTANT_ID" \
  -H "Authorization: Bearer $VAPI_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": { "provider": "xai", "model": "grok-4.3" }
  }'
```

## Text-to-speech

Set `voice.provider` to `xai` and `voice.voiceId` to a voice.

| Voice | `voiceId` |
| -- | -- |
| Eve (default) | `eve` |
| Ara | `ara` |
| Rex | `rex` |
| Sal | `sal` |
| Leo | `leo` |

```json
{
  "voice": { "provider": "xai", "voiceId": "eve" }
}
```

Supported languages:

| Code | Language |
| -- | -- |
| `auto` | Automatic detection |
| `en` | English |
| `ar-EG` | Arabic (Egypt) |
| `ar-SA` | Arabic (Saudi Arabia) |
| `ar-AE` | Arabic (United Arab Emirates) |
| `bn` | Bengali |
| `zh` | Chinese |
| `fr` | French |
| `de` | German |
| `hi` | Hindi |
| `id` | Indonesian |
| `it` | Italian |
| `ja` | Japanese |
| `ko` | Korean |
| `pt-BR` | Portuguese (Brazil) |
| `pt-PT` | Portuguese (Portugal) |
| `ru` | Russian |
| `es-MX` | Spanish (Mexico) |
| `es-ES` | Spanish (Spain) |
| `tr` | Turkish |
| `vi` | Vietnamese |

## Speech-to-text

Set `transcriber.provider` to `xai`. xAI exposes a single speech-to-text model, `default`.

```json
{
  "transcriber": { "provider": "xai", "model": "default" }
}
```

Supported languages:

| Code | Language |
| -- | -- |
| `ar` | Arabic |
| `cs` | Czech |
| `da` | Danish |
| `nl` | Dutch |
| `en` | English |
| `fil` | Filipino |
| `fr` | French |
| `de` | German |
| `hi` | Hindi |
| `id` | Indonesian |
| `it` | Italian |
| `ja` | Japanese |
| `ko` | Korean |
| `mk` | Macedonian |
| `ms` | Malay |
| `fa` | Persian |
| `pl` | Polish |
| `pt` | Portuguese |
| `ro` | Romanian |
| `ru` | Russian |
| `es` | Spanish |
| `sv` | Swedish |
| `th` | Thai |
| `tr` | Turkish |
| `vi` | Vietnamese |

## Related

<CardGroup cols={2}>
  <Card title="xAI documentation" href="https://docs.x.ai">
    The provider's own API reference.
  </Card>
  <Card title="Model providers" href="/providers/model/overview">
    Compare every LLM provider Vapi supports.
  </Card>
</CardGroup>
