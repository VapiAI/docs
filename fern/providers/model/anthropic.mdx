---
title: Anthropic
subtitle: Use Anthropic's Claude models as the language model for your Vapi voice agent.
description: Connect Anthropic to Vapi to use Claude models as your voice agent's language model. Covers the supported Claude models and how to use them in the API.
slug: providers/model/anthropic
---

Anthropic is a large language model provider whose Claude models generate your voice agent's responses. Connect Anthropic to use Claude on a Vapi assistant. This page covers what Anthropic is and the models it supports.

## What is Anthropic

Anthropic builds the Claude family of large language models. On Vapi, you select a Claude model as the LLM for a voice agent.

You can use Anthropic through Vapi's default integration, or use your own Anthropic account by connecting it in [Integrations](/providers/integrations). To run the same Claude models through AWS instead, see [Anthropic Bedrock](/providers/model/anthropic-bedrock).

## Language model

Set `model.provider` to `anthropic` and `model.model` to a Claude model.

```bash
curl -X PATCH "https://api.vapi.ai/assistant/ASSISTANT_ID" \
  -H "Authorization: Bearer $VAPI_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": { "provider": "anthropic", "model": "claude-opus-4-6" }
  }'
```

For additional API configuration options, review the [`AnthropicModel` fields](/api-reference/assistants/create#request.body.model.AnthropicModel) in the Create Assistant API reference.

| Model | Model ID |
| --- | --- |
| Claude Opus 4.6 | `claude-opus-4-6` |
| Claude Opus 4.5 | `claude-opus-4-5-20251101` |
| Claude Sonnet 5 | `claude-sonnet-5` |
| Claude Sonnet 4.6 | `claude-sonnet-4-6` |
| Claude Sonnet 4.5 | `claude-sonnet-4-5-20250929` |
| Claude Haiku 4.5 | `claude-haiku-4-5-20251001` |

## Related

<CardGroup cols={2}>
  <Card title="Anthropic Bedrock" icon="brain" href="/providers/model/anthropic-bedrock">
    Run the same Claude models through AWS Bedrock.
  </Card>
  <Card title="Model providers" icon="brain" href="/providers/model/overview">
    Compare every LLM provider Vapi supports.
  </Card>
</CardGroup>
